testcases:
  - title: "[should fail] table structure test"
    exec:
      - CREATE TABLE my_table (b TEXT, a INT)
    table: my_table
    check:
      columns: [name]
      equal_to:
        - name: a
        - name: b